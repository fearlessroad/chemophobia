<div id="itemcontainer">
	<div id="all_items_box">
		<h3>All Items</h3>
		<% if @items %>
		<% @items.each do |i|%>
		<a href="/items/display/<%= i.id%>"><div class="all_items_pic"><%= image_tag(i.image)%></div></a>
		<% end %>
		<% end %>
	</div>
</div>