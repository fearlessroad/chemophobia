<%= render partial: '/shared/header.html.erb'%>
<div class="other">
	<div class="items">
		<h3>All Items</h3>
		<% if @items %>
		<% @items.each do |i|%>
		<a href="/items/show/<%= i.id%>"><%= image_tag(i.image)%></a>
		<% end %>
		<% end %>
	</div>
</div>
